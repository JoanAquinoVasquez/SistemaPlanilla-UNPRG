{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "start:both": "concurrently \"php artisan serve\" \"cd ../client && npm run dev\""
    },
    "devDependencies": {
        "axios": "^1.7.4",
        "concurrently": "^9.1.0",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}
