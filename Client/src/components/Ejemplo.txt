import Breadcrumb from "../../components/Breadcrumb/Breadcrumb";


export default function LeyesParametros() {
  return (
    <div>
      <Breadcrumb
        paths={[
          { name: "Leyes y Parámetros" },
        ]}
      />
      <div className="bg-white rounded-lg p-4 shadow-md mt-5">
        <h1>LeyesParametros</h1>
      </div>
    </div>
  );
}

import ExportarExcelButton from "../../components/ExportExcel/ExportarExcelButton";

<Button color="primary" endContent={<PlusIcon />} className="w-full sm:w-auto" onPress={onOpen}>
            Nuevo
          </Button>
          <ExportarExcelButton /> 
          {/* <Button auto light className="w-full sm:w-auto" onClick={exportarExcel}>
            <FaUpload className="mr-2" />
            Exportar
          </Button> */}